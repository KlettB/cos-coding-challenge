import React from 'react'
import {shallow} from 'enzyme'

import SearchFilterBarComponent from "./SearchFilterBarComponent";

describe('SearchFilterBarComponent', () => {

    it('should render', () => {
        shallow(<SearchFilterBarComponent />)
    })

})
